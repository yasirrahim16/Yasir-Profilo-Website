# Installation

1. Extract the ZIP.
2. Open a terminal in the extracted folder.
3. Run `npm install`.
4. Run `npm run dev`.
5. Open `http://localhost:3000`.

For a production check use `npm run build` and then `npm run start`.
