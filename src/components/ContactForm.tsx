"use client";
import { FormEvent, useState } from "react";
import { ArrowUpRight, Check } from "lucide-react";
import { site } from "@/lib/content";
export default function ContactForm(){const [sent,setSent]=useState(false);const onSubmit=(e:FormEvent<HTMLFormElement>)=>{e.preventDefault();const form=new FormData(e.currentTarget);const name=String(form.get('name')||'');const email=String(form.get('email')||'');const message=String(form.get('message')||'');if(!name||!email||!message)return;const endpoint=process.env.NEXT_PUBLIC_CONTACT_ENDPOINT;if(endpoint){fetch(endpoint,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({name,email,message})}).catch(()=>{});}else{window.location.href=`mailto:${site.email}?subject=${encodeURIComponent(`Portfolio enquiry from ${name}`)}&body=${encodeURIComponent(`${message}\n\nReply-to: ${email}`)}`;}setSent(true);};return <form onSubmit={onSubmit} className="space-y-4"><div className="grid gap-4 sm:grid-cols-2"><input name="name" required placeholder="Your name" className="h-14 rounded-2xl border border-white/10 bg-white/[.025] px-4 outline-none transition placeholder:text-white/25 focus:border-cyan/35"/><input name="email" required type="email" placeholder="Email address" className="h-14 rounded-2xl border border-white/10 bg-white/[.025] px-4 outline-none transition placeholder:text-white/25 focus:border-cyan/35"/></div><textarea name="message" required placeholder="Tell me about the idea..." rows={7} className="w-full rounded-2xl border border-white/10 bg-white/[.025] px-4 py-4 outline-none transition placeholder:text-white/25 focus:border-cyan/35"/><button type="submit" className="group inline-flex items-center gap-3 rounded-full bg-white px-6 py-3.5 text-sm font-semibold text-black transition hover:bg-cyan">{sent?<><Check className="h-4 w-4"/> Message ready</>:<>Send enquiry <ArrowUpRight className="h-4 w-4 transition group-hover:translate-x-0.5 group-hover:-translate-y-0.5"/></>}</button></form>}
